// 这是一个模拟的WASM文件
// 在实际项目中，这应该是编译后的WebAssembly二进制文件
// 为了演示目的，我们创建一个文本文件来表示WASM模块

// WASM模块导出的函数:
// 1. hello(name: string) -> string
// 2. calculate(a: number, b: number) -> number

// 注意：这只是一个占位符文件
// 真实的WASM文件应该是通过Rust、C++或其他语言编译生成的二进制文件
// 在实际实现中，主进程需要加载和执行真正的WASM模块

// 模拟的函数签名:
// export function hello(name: string): string {
//     return `Hello, ${name}! 来自CubeModule插件的问候。`;
// }
//
// export function calculate(a: number, b: number): number {
//     return a + b;
// }

// WASM模块元数据
{
  "module_name": "test-cubemodule",
  "version": "1.0.0",
  "exports": [
    {
      "name": "hello",
      "type": "function",
      "params": ["string"],
      "returns": "string"
    },
    {
      "name": "calculate",
      "type": "function",
      "params": ["number", "number"],
      "returns": "number"
    }
  ]
}